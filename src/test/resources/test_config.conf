jdbc {
    driver = "org.h2.Driver"
    uri = "jdbc:h2:mem:test;TRACE_LEVEL_SYSTEM_OUT=3"
    limit = 100
}

mongo {
    dry-run = true
    host = "127.0.0.1"
    database = "migration"
    clean = false
}
